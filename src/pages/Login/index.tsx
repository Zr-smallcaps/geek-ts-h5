import React from 'react'
import { NavBar, Form, Input, Button, Toast } from 'antd-mobile'

function Login() {
	return (
		<div>
			<Button type="submit" className="login-submit" color="primary" block>
				登录
			</Button>
		</div>
	)
}

export default Login
